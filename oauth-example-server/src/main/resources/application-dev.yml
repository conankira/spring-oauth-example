#服务器接口配置
server:
  port: 9090
  tomcat.max-threads: 0
  tomcat.uri-encoding: UTF-8
  servlet:
    session:
      timeout: 30
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/spring_boot_example?useUnicode=true&characterEncoding=utf-8
    username: root
    password: root

  redis:
    password: 123456w
    database: 6
    url: redis://123456w@alpha.zyyl.redis01.srv:6378/6?timeout=5s
    timeout: 5000

logging.level.org.springframework.security: DEBUG
log:
  path: ${user.home}/logs/applogs/oauth_server
  level: info

